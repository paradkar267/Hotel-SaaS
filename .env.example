# Optional production bootstrap restriction.
# The first authenticated identity may initialize HotelOS only when it matches.
BOOTSTRAP_ADMIN_EMAIL=owner@example.com

# Runtime resources are bound by the hosting platform, not stored as secrets here:
# DB      -> Cloudflare D1
# BUCKET  -> private Cloudflare R2
